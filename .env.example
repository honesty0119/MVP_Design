# auto：有密钥时调用真实 LLM；无密钥时使用规则回退，保证 Demo 可运行。
AGENT_LLM_MODE=auto

# 与参考 Agent 项目一致，使用 DashScope OpenAI-compatible 接口。
AGENT_LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# 密钥只放在操作系统环境变量，不要写进本文件。
AGENT_LLM_API_KEY_ENV=OPENAI_API_KEY
AGENT_LLM_MODEL=qwen-plus
