# AI-coding 使用说明

## 使用工具与参与环节

本作业使用 Codex 辅助完成：需求拆解、状态机与表结构草拟、API/页面代码生成、边界测试、README 和业务文档排版。我的职责是确定业务口径、审查生成结果、收缩范围并验证可运行性。

## AI 输出中发现的问题

初始生成思路容易把状态当成可任意修改的枚举，忽略“有效线索不能直接跳 SQL”、MQL/SQL 准入条件、无效原因必填和历史追溯；指标也容易只算状态数量，不说明分母与周期。此外，第三方回调如果只按手机号更新，会在重复投递时产生重复副作用。

## 两个真实 Prompt 与人工修正

### Prompt 1：状态流转

> 请为市场线索系统设计状态流转，覆盖新线索、外呼、有效/无效、加微、MQL 和 SQL，并生成状态修改接口。

AI 初稿把 `status` 设计成任意字符串更新，前端可以直接从新线索选择 SQL。这个输出“功能上能改状态”，但会破坏漏斗和阶段耗时，因此没有直接采用。我将允许路径收口到服务端 `ALLOWED_TRANSITIONS`，并补充三条准入规则：无效原因必填；MQL 需要负责人和跟进；SQL 只能由 MQL 转化且需要商机说明。对应测试验证跨级跳转会被拒绝。

### Prompt 2：第三方外呼回调

> 模拟外呼系统回调，根据手机号和接通结果修改线索状态。

AI 初稿建议收到回调后直接按手机号更新状态。这里有两个问题：第三方通常至少投递一次，同一事件可能重复；延迟回调还可能把已经进入 MQL 的线索拉回“已接通”。我改为要求 `event_id`，使用唯一约束实现幂等；只有当前状态为“待外呼”时才允许推进到“已接通/未接通”，其他状态只记录事件、不回滚业务状态。

## 我的判断与修正

- 将状态更新收口到服务端状态机，不由前端决定合法性。
- MQL 要求已分配负责人且至少有一次跟进；SQL 必须从 MQL 转化并填写商机说明。
- 在硬性准入之外增加线索评分：联系方式有效性、负责人、跟进事实、渠道意向、私域承接和跟进时效共同解释“为什么建议/不建议转 MQL”。评分只做业务解释，不替代服务端状态机。
- 无效必须填写原因；重新打开要求备注，并要求主管角色，生产环境还需审批流和复核意见。
- 状态变化写入独立历史表，跟进记录不覆盖，便于审计和计算阶段耗时。
- 增加轻量角色模拟：销售只能操作自己负责的线索，主管可转移负责人和复核无效线索；关键写操作写入审计记录。
- 回调以三方 `event_id` 唯一约束实现幂等；手机号唯一约束用于线索去重，两者不混用。
- 明确漏斗为“当前存量快照”口径，不把 Demo 指标冒充正式经营口径。
- 使用 Python 标准库实现，避免评审环境因依赖安装失败而无法启动。
- 参考最小 Agent Runtime 的 LLM→工具→结果回填循环，接入 OpenAI-compatible API；LLM 只做意图判断和工具选择，不拥有原始 SQL 执行能力。

## Agent 接入的设计判断

自然语言查询不是把用户问题直接交给 Text-to-SQL。当前 Agent 暴露三个 JSON Schema 工具：渠道 SQL 转化率、超时线索和漏斗快照。模型选择工具并生成结构化参数，服务端校验参数后执行固定的参数化查询，再把真实结果回填给模型生成最终回答。

这样设计的边界是：

- API Key 只从环境变量读取，不进入代码、日志或 Git。
- 最大只执行 3 个工具调用，未知工具与越界参数直接拒绝。
- 不开放表名、字段名和 SQL 字符串参数，避免危险查询和口径漂移。
- 无密钥或模型网络失败时回退到确定性规则，页面明确标注“规则回退”，不冒充 LLM。
- 页面展示 Agent 执行链路：收到问题、选择工具、参数、服务端校验、结果回填和数据边界，便于现场解释 AI 参与了什么。
- 自动化测试使用脚本化模型响应验证 Tool Calling 循环，不消耗真实 API。

## 验证

实际启动服务并通过浏览器检查主流程；自动化测试覆盖手机号去重、禁止跨级跳转、无效原因必填、回调幂等、完整的新线索到 SQL 流程、线索评分解释、销售跨负责人权限拒绝，以及 Agent Tool Calling 和规则回退。所有演示数据均为虚构号码。

## 如果再给 1 天

1. 将当前角色模拟接入真正登录、会话、部门/团队数据范围和审批流。
2. 将导入改为异步任务，提供逐行错误报告，并增加回调签名、重试与死信队列。
3. 增加 cohort 漏斗、阶段停留时长、渠道成本与归因口径，避免只看存量快照。
